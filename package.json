{
  "name": "td",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "dev": "PARCEL_ELM_NO_DEBUG=1 parcel src/index.html --port 3000",
    "dev:windows": "set PARCEL_ELM_NO_DEBUG=1 &&parcel src/index.html --port 3000",
    "dev:debug": "parcel src/index.html --port 3000",
    "build": "npm run generate && parcel build src/index.html",
    "generate": "elm-codegen run --output=src",
    "generate:watch": "elm-codegen run --output=src --watch"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@parcel/transformer-elm": "^2.8.2",
    "parcel": "^2.8.2"
  },
  "dependencies": {
    "elm": "^0.19.1-5",
    "elm-codegen": "^0.2.0"
  }
}
